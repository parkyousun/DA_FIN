---
title: "차원축소"
author: "202021540 박유선"
date: '2021 6 20 '
output: html_document
---
```{r}
apt<- read.csv("apt_final_1.csv")

sum(is.na(apt))

str(apt)

table(apt$`최근.매매.연도`)

apt$` 최근 매매 연도`<- as.character(apt$` 최근 매매 연도`)

apt$`최근 매매 월`<- as.factor(apt$`최근 매매 월`)
```

